# pnpm Configuration
# Ensures consistent dependency installation across environments

# Save exact versions (no ^ or ~ prefixes)
save-exact=true

# Don't hoist dependencies (strict dependency resolution)
# Using false for better compatibility with Next.js
shamefully-hoist=false

# Disable strict peer dependencies (some packages have incorrect peer deps)
strict-peer-dependencies=false
